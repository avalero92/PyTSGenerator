# PyTSGenerator — dependencias Python
# Instalación: pip install -r requirements.txt
#
# Se recomienda usar un entorno virtual:
#   python -m venv .venv && source .venv/bin/activate  (Linux/macOS)
#   python -m venv .venv && .venv\Scripts\activate     (Windows)

# ── Geoespacial ──────────────────────────────────────────────────────
rasterio>=1.3.0
geopandas>=0.14.0
pyproj>=3.6.0
shapely>=2.0.0
fiona>=1.9.0

# ── Cálculo numérico ─────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0

# ── API Copernicus HDA (WEkEO) ───────────────────────────────────────
hda>=1.20

# ── Almacenamiento seguro de credenciales (opcional pero recomendado) ─
keyring>=24.0.0
